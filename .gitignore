node_modules/
dist/
site-dist/
site/public/
coverage/
coverage-upload/
junit/
*.tsbuildinfo
*.local
.DS_Store

# Shared coverage tooling, fetched on demand by `make coverage`.
.coverage-report.py

# IDE
.idea/
*.iml

# Local secrets — never commit. Only the signing key + service-account JSON are secret;
# non-secret IDs live in the committed cws.json.
secrets/
*service-account*.json
.env
.env.*

# Release artifacts (regenerated by CI / the Makefile)
*.zip
*.crx
updates.xml

# crx signing key — never commit; store as the CRX_PRIVATE_KEY secret instead
key.pem
*.pem
