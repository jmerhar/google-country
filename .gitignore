node_modules/
dist/
site-dist/
site/public/
coverage/
coverage-upload/
*.tsbuildinfo
*.local
.DS_Store

# IDE
.idea/
*.iml

# Local secrets (auto-loaded by the Makefile) — never commit
.env
.env.*
!.env.example
secrets/
*service-account*.json

# Release artifacts (regenerated by CI / the Makefile)
*.zip
*.crx
updates.xml

# crx signing key — never commit; store as the CRX_PRIVATE_KEY secret instead
key.pem
*.pem
